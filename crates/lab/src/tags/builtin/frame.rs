def! {
  frame,
  Frame,
  FrameArg,
  doc:
  "zh-CN" = r#####"`<frame>` [doc](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/frame)

---

**允许的内容**



**是否可忽略关闭标签**



**父标签**



[Dom API]()


---

## 属性

 包含全局属性: true

- frameborder

	这个属性允许你为帧添加边框。
- marginheight

	这个属性定义了帧之间的边距有多高。
- marginwidth

	这个属性定义了帧之间的边距有多宽。
- name

	这个属性用于给帧添加标签。如果没有标签，所有链接都会在它们所在的帧中打开。
- noresize

	这个属性避免让用户改变帧的大小。
- scrolling

	这个属性定义了滚动条的存在。如果不使用这个属性，浏览器会按需放置滚动条。有两个选择：<code>"yes"</code>&nbsp;用于展示滚动条，即使是多余的；<code>"no"</code>&nbsp;用于不展示滚动条，即使它是必要的。
- src

	&nbsp;这个属性指定了由帧展示的文档。"#####
}

